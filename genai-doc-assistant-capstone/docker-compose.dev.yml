# Dev overrides: mount source code for hot reload (no image rebuild needed)
#
# From repo root:
#   docker compose -f genai-doc-assistant-capstone/docker-compose.yml -f genai-doc-assistant-capstone/docker-compose.dev.yml up --build
#
# From this directory:
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build

services:
  backend:
    volumes:
      - .:/app
      - ../pycorekit:/workspace/pycorekit
    command: >
      sh -c "pip install -e /workspace/pycorekit &&
             uvicorn main:app --host 0.0.0.0 --port 8000 --reload"

  streamlit:
    volumes:
      - ./front-end/streamlit:/app
